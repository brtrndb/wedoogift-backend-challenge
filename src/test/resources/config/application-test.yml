spring:
  application:
    name: Wedoogift Deposit Application Test
  jpa:
    show-sql: true
    properties:
      hibernate:
        show_sql: false
        format_sql: true

logging:
  config: classpath:logback-test.xml

debug: off
